[project]
name = "flam-submission"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "tiktoken>=0.13.0",
    "transformers>=5.14.1",
]
